<!-- A form will be on this page for start time and end time
	Subtract the end time from the start time to get the total amount of hours desired
	Create a new row in the Appointments database for each hour
	IE start time is 08:00 and end time is 17:00, so 9 hour work day, 9 rows in the db for that day
-->



<%= form_for @appointment do |f| %>
	<p>
		<%= f.label :date %>
		<%= f.date_field :appt_date %>
	</p>

	<p>
		<%= f.label :start_time %>
		<%= f.time_field :start_time %>
	</p>

	<p>
		<%= f.label :end_time %>
		<%= f.time_field :end_time %>
	</p>

	<p><%= f.submit %></p>
<% end %>
<!--

<%= text_area_tag(:message, "Hi, nice site", size: "24x6") %>
<%= password_field_tag(:password) %>
<%= hidden_field_tag(:parent_id, "5") %>
<%= search_field(:user, :name) %>
<%= telephone_field(:user, :phone) %>
<%= date_field(:user, :born_on) %>
<%= datetime_field(:user, :meeting_time) %>
<%= datetime_local_field(:user, :graduation_day) %>
<%= month_field(:user, :birthday_month) %>
<%= week_field(:user, :birthday_week) %>
<%= url_field(:user, :homepage) %>
<%= email_field(:user, :address) %>
<%= color_field(:user, :favorite_color) %>
<%= time_field(:task, :started_at) %>
<%= number_field(:product, :price, in: 1.0..20.0, step: 0.5) %>
<%= range_field(:product, :discount, in: 1..100) %>
-->